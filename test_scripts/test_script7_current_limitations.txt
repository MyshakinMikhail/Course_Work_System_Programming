CREATE DATABASE limitations_demo;
USE limitations_demo;
CREATE TABLE users (id int INDEXED, name string, age int);
INSERT INTO users (id, name, age) VALUE (1, "Ann", 20), (2, "Bob", 30);
UPDATE users SET name = "Alice" WHERE id == 1;
DELETE FROM users WHERE id == 2;
INSERT INTO users (id, name) VALUE (3, "Carl");
INSERT INTO users (id, name, age) VALUE (4, NULL, 40);
SELECT * FROM users;

USE missing_database;
CREATE DATABASE errors_demo;
USE errors_demo;
CREATE TABLE users (id int INDEXED, name string);
CREATE TABLE users (id int INDEXED, name string);
INSERT INTO users (id, name) VALUE (1, "Ann");
INSERT INTO users (id, missing) VALUE (2, "Bob");
SELECT (missing) FROM users;
SELECT * FROM users;

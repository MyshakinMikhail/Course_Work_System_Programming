# core_storage_tests

Интеграционные unit-тесты для связки `core` + `storage`.

Проверяется базовый сценарий работы таблицы:

- создание таблицы и запись схемы в файл;
- вставка записей через `Table`;
- передача `offset` в индекс при вставке;
- чтение всех записей через `scan()`.
